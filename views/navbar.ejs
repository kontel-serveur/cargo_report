<nav class="navbar">
  <div class="navbar-container">
    <div class="navbar-brand">
      <img src="../assets/logo.png" style="width: 40%; height: auto;" />
    </div>
    <button class="navbar-toggle" aria-label="Toggle navigation">
      <span class="toggle-bar"></span>
      <span class="toggle-bar"></span>
      <span class="toggle-bar"></span>
    </button>
    <ul class="navbar-menu">
      <li><a href="/accueil" class="navbar-link">Accueil</a></li>
      <li><a href="/enregistrement" class="navbar-link">Enregistrer</a></li>
      <li><a href="/compte" class="navbar-link">Contact</a></li>
      <li><a href="/" class="navbar-link">Se deconnecter</a></li>
    </ul>
  </div>
</nav>

<nav class="sidebar">
  <div class="sidebar-container">
    <div class="sidebar-brand">
      <img src="../assets/logo.png" style="width: 100%; height: auto; margin-bottom: 40px;" />
    </div>

    <button class="sidebar-toggle" aria-label="Toggle sidebar">
      <span class="toggle-bar"></span>
      <span class="toggle-bar"></span>
      <span class="toggle-bar"></span>
    </button>
    <ul class="sidebar-menu">
      <li><a href="/accueil" class="sidebar-link">Accueil</a></li>
      <li><a href="/enregistrement" class="sidebar-link">Enregistrer</a></li>
      <li><a href="/compte" class="sidebar-link">Profile</a></li>
      <li><a href="/" class="sidebar-link">Deconnexion</a></li>
  </ul>
  


  </div>
</nav>

<script src="/scripts/navbar.js"></script>

<style>
body, ul, li, a, button {
  margin: 0;
  padding: 0;
  list-style: none;
  text-decoration: none;
  box-sizing: border-box;
}

/* Navbar container */
.navbar {
  background-color: #60c7c1;
  color: white;
  font-family: 'Arial', sans-serif;
  padding: 10px 20px;
  border-bottom: 2px solid #3aa5a1;
}

.navbar-container {
  display: flex;
  justify-content: space-between;
  align-items: center;
  max-width: 1200px;
  margin: 0 auto;
}

/* Brand/logo */
.navbar-brand .navbar-logo {
  font-size: 24px;
  font-weight: bold;
  color: white;
}

/* Navbar menu */
.navbar-menu {
  display: flex;
  gap: 20px;
}

.navbar-menu li {
  list-style: none;
}

.navbar-link {
  color: white;
  font-size: 16px;
  font-weight: 500;
  transition: color 0.3s ease;
}

.navbar-link:hover {
  color: #e3f9f8;
}



.sidebar-container {
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
}

.sidebar-menu {
  display: flex;
  flex-direction: column;
  gap: 15px;
}

.sidebar-link {
  color: white;
  font-size: 16px;
  font-weight: 500;
  transition: color 0.3s ease;
}

.sidebar-link:hover {
  color: #e3f9f8;
}

.sidebar-toggle {
  display: none;
}



.navbar {
  background-color: #60c7c1;
  color: white;
  font-family: 'Arial', sans-serif;
  padding: 10px 20px;
  border-bottom: 2px solid #3aa5a1;
  display: none; /* Hide the navbar by default on larger screens */
}

/* Sidebar container */
.sidebar {
  position: fixed;
  top: 0;
  left: 0; /* Sidebar visible on larger screens */
  height: 100%;
  width: 150px;
  background-color: #60c7c1;
  color: white;
  font-family: 'Arial', sans-serif;
  padding: 20px;
  border-right: 2px solid #3aa5a1;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  z-index: 5;
}

/* Responsive Styles for Navbar */
@media (max-width: 768px) {
  .navbar {
    display: block; /* Show navbar on small screens */
  }

  .sidebar {
    left: -250px; /* Initially hide sidebar on small screens */
    display: none;
  }

  .navbar-menu {
    display: none;
    flex-direction: column;
    background-color: #60c7c1;
    position: absolute;
    top: 60px;
    right: 20px;
    width: 200px;
    border-radius: 5px;
    overflow: hidden;
    z-index: 10;
  }

  .navbar-menu.active {
    display: flex;
  }

  .navbar-toggle {
    display: flex;
  }

  .sidebar.active {
    left: 0; /* Show sidebar on toggle */
  }
}

/* Responsive Styles for Sidebar */
@media (min-width: 769px) {
  .sidebar {
    left: 0; /* Sidebar is always shown on larger screens */
  }

  .sidebar-toggle {
    display: none; /* Hide sidebar toggle on larger screens */
  }

  .navbar-menu {
    display: flex;
  }
}

/* Sidebar items styling */
.sidebar-menu li {
            padding: 15px 20px;
        }

        /* Sidebar link styling */
        .sidebar-link {
            display: block;
            color: white;
            font-size: 17px;
            text-decoration: none;
            font-weight: bold;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        .sidebar-link:hover {
            background-color: #2980b9; /* Hover effect */
            color: #ecf0f1;
            padding-left: 10px; /* Slight padding effect on hover */
        }

        .sidebar-link.active {
            background-color: #34495e;
            color: #ecf0f1;
        }

        /* Active link state styling */
        .sidebar-menu li.active {
            background-color: #34495e;
        }

        /* Optional: Icon addition for better readability */
        .sidebar-link::before {
            content: '\2022'; /* Unicode bullet symbol */
            margin-right: 10px;
            font-size: 20px;
        }

        /* Responsive sidebar for mobile screens */
        @media (max-width: 768px) {
            .sidebar-menu {
                width: 100%;
                height: auto;
                position: relative;
            }

            .sidebar-menu li {
                padding: 10px 15px;
            }
        }

</style>